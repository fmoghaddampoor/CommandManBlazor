<Window x:Class="CommandMan.Tray.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CommandMan.Tray.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="CommandMan Server" Height="250" Width="350"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="White"
        Icon="App.ico">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="#555555" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#555555"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222222"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
            <Ellipse Width="12" Height="12" Fill="{Binding StatusColor}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding StatusText}" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Configuration -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#AAAAAA"/>
            <TextBox Text="{Binding PortText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsPortEnabled}"
                     Width="60" Height="24" 
                     Background="#2D2D2D" Foreground="White" BorderBrush="#555555" 
                     VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <!-- Content/Actions -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <Button Command="{Binding OpenCommand}" Height="40" Width="200" Background="#007ACC" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M10,20V14H14V20H19V12H22L12,3L2,12H5V20H10Z" Fill="White" Height="14" Width="14" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Open CommandMan"/>
                </StackPanel>
            </Button>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding StartCommand}" Width="95" Background="#288735">
                    <StackPanel Orientation="Horizontal">
                         <Path Data="M8,5.14V19.14L19,12.14L8,5.14Z" Fill="White" Height="12" Width="12" Stretch="Uniform" Margin="0,0,5,0"/>
                         <TextBlock Text="Start"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding StopCommand}" Width="95" Background="#bc2535">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M18,18H6V6H18V18Z" Fill="White" Height="12" Width="12" Stretch="Uniform" Margin="0,0,5,0"/>
                         <TextBlock Text="Stop"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
        
        <!-- Footer -->
        <TextBlock Text="{Binding FooterText}" Grid.Row="3" Foreground="#888888" HorizontalAlignment="Center" FontSize="11"/>
        
    </Grid>
</Window>

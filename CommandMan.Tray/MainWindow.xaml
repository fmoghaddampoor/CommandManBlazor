<Window x:Class="CommandMan.Tray.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CommandMan Server" Height="250" Width="350"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="White"
        Icon="App.ico">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444444"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <Ellipse Width="12" Height="12" x:Name="StatusDot" Fill="Gray" Margin="0,0,10,0"/>
            <TextBlock x:Name="StatusText" Text="Checking Status..." FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Content/Actions -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <Button x:Name="BtnOpen" Click="BtnOpen_Click" Height="40" Width="200" Background="#007ACC" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M10,20V14H14V20H19V12H22L12,3L2,12H5V20H10Z" Fill="White" Height="14" Width="14" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Open CommandMan"/>
                </StackPanel>
            </Button>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnStart" Click="BtnStart_Click" Width="95" Background="#28a745">
                    <StackPanel Orientation="Horizontal">
                         <Path Data="M8,5.14V19.14L19,12.14L8,5.14Z" Fill="White" Height="12" Width="12" Stretch="Uniform" Margin="0,0,5,0"/>
                         <TextBlock Text="Start"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BtnStop" Click="BtnStop_Click" Width="95" Background="#dc3545">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M18,18H6V6H18V18Z" Fill="White" Height="12" Width="12" Stretch="Uniform" Margin="0,0,5,0"/>
                         <TextBlock Text="Stop"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
        
        <!-- Footer -->
        <TextBlock Grid.Row="2" Text="Running on port 5000" Foreground="#888888" HorizontalAlignment="Center" FontSize="11"/>
        
    </Grid>
</Window>
